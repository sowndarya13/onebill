import React, { useState } from "react";
import Button from "./button";
const footer = {
  marginLeft: 550,
  marginTop: 20,
};
function Footer() {
  // let [page, Changepage] = useState(1);

  return (
    <div>
      <footer style={footer}>page 1</footer>
    </div>
  );
}

export default Footer;
